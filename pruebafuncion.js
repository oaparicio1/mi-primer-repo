function persona(nombre, appelido){

    let cadena = "nombre es " + nombre + " y el apelido es " + appelido;

    return cadena;
}


let fullname = persona("omar", "aparicio");

console.log(fullname);